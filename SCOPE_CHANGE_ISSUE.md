# Scope change: repository direction updated

This repository has changed direction.

## Summary

- `main` now hosts the final `pretext-3d` project.
- The previous repository contents were preserved in `old-version-backup`.
- The old version remains available only as a reference and is no longer the active project direction.

## Why

The project scope was updated and the team decided to move forward with a different final deliverable.

## Notes

- Anyone looking for the previous work should use the `old-version-backup` branch.
- New development and review should target `main`.
